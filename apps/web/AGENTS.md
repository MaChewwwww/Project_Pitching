# Agent rules — apps/web

The repository-wide rules live in [`../../AGENTS.md`](../../AGENTS.md). Read those first;
they cover the ID scheme, git convention, Definition of Done, and the hard rules. This file
carries only what is specific to this Next.js app.

- Design tokens and the component inventory: [`../../docs/design.md`](../../docs/design.md).
- `components/ui/` holds shadcn primitives installed by `make shadcn`. **Do not edit them**
  except for token wiring (NFR-MNT-006) — app-specific styling belongs in `components/common/`.
- Never import a raw `ui/` primitive into a page. Pages import from `common/` or `features/`.
- Build at 360px first. Every component must work there before it is considered done.

Everything below is generated by `next dev` and re-added automatically if removed.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
