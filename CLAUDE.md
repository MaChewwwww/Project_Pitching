# Claude Code / Cowork

See [`AGENTS.md`](./AGENTS.md) — the single source of truth for agent rules in this repo.
Everything (read order, ID scheme, git convention, Definition of Done, repo layout, hard
rules) lives there so Claude, Gemini, and any other agent follow the same rules instead of
three drifting copies.
